{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":automergeMinor",
    ":automergeDigest",
    ":enablePreCommit",
    ":semanticCommitScopeDisabled",
    ":semanticCommitTypeAll(build)"
  ],
  "description": [
    "minimumReleaseAge holds a version bump for 3 days. Automerge reaches root-equivalent containers and gitops_reconcile applies a merge within ~15 min, so an unheld bump could be live in one cycle with nobody in the loop.",
    "internalChecksFilter is strict, so a too-young version opens no PR at all. vulnerabilityAlerts clears minimumReleaseAge with prCreation immediate, so an OSV fix still lands at once. Digest updates and container images are exempt — see the rules below.",
    "automergeSchedule constrains merging, not PR creation: bumps still open and run CI when ready, they just cannot land mid-session and move main out from under an operator branch. Each collision costs a rebase and a full CI re-run, hetzner tier included. vulnerabilityAlerts clears the window as well as the age hold."
  ],
  "semanticCommits": "enabled",
  "timezone": "Europe/London",
  "osvVulnerabilityAlerts": true,
  "minimumReleaseAge": "3 days",
  "automergeSchedule": ["after 1am and before 5am"],
  "vulnerabilityAlerts": {
    "automergeSchedule": null
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Container image refs pinned in role vars; quadlet templates render them.",
      "managerFilePatterns": ["/^roles/.+/vars/main\\.yml$/"],
      "matchStrings": ["image: (?<depName>[^\\s:]+):(?<currentValue>[^\\s@]+)(?:@(?<currentDigest>sha256:\\S+))?"],
      "autoReplaceStringTemplate": "image: {{{depName}}}:{{{newValue}}}{{#if newDigest}}@{{{newDigest}}}{{/if}}",
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "The disaster-recovery runbook hardcodes the mariadb roles/wordpress pins, so its fallback SQL load runs on that engine. Same depName, so both bump in one PR.",
      "managerFilePatterns": ["/^docs/disaster-recovery\\.md$/"],
      "matchStrings": ["(?<depName>docker\\.io/library/mariadb):(?<currentValue>[^\\s@]+)(?:@(?<currentDigest>sha256:\\S+))?"],
      "autoReplaceStringTemplate": "{{{depName}}}:{{{newValue}}}{{#if newDigest}}@{{{newDigest}}}{{/if}}",
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "Grafana dashboard revisions pinned in role vars; the role fetches them at converge.",
      "managerFilePatterns": ["/^roles/.+/vars/main\\.yml$/"],
      "matchStrings": ["# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+) packageName=(?<packageName>\\S+)\\s+\\S+: (?<currentValue>\\d+)"]
    },
    {
      "customType": "regex",
      "description": "Packer for the lint gate's packer-fmt/packer-validate hooks; no zypper package, and it cannot have one — cracklib owns the name.",
      "managerFilePatterns": ["/^\\.github/workflows/lint\\.yml$/"],
      "matchStrings": ["# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+version: \"(?<currentValue>[^\"]+)\""],
      "extractVersionTemplate": "^v(?<version>.+)$"
    },
    {
      "customType": "regex",
      "description": "Packer plugins, pinned exactly because packer init writes no lock file.",
      "managerFilePatterns": ["/^packer/.+\\.pkr\\.hcl$/"],
      "matchStrings": ["# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+version = \"(?<currentValue>[^\"]+)\""],
      "extractVersionTemplate": "^v(?<version>.+)$"
    },
    {
      "customType": "regex",
      "description": "butane for the lint gate; the binary comes out of a digest-pinned image because the GitHub release ships signatures but no checksums.",
      "managerFilePatterns": ["/^\\.github/workflows/lint\\.yml$/"],
      "matchStrings": ["image=(?<depName>[^\\s:]+):(?<currentValue>[^\\s@]+)(?:@(?<currentDigest>sha256:\\S+))?"],
      "autoReplaceStringTemplate": "image={{{depName}}}:{{{newValue}}}{{#if newDigest}}@{{{newDigest}}}{{/if}}",
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "tflint for CI and the workstation; no zypper package. One depName, so tflint_version and dev_tflint_version bump together.",
      "managerFilePatterns": ["/^\\.github/workflows/lint\\.yml$/", "/^roles/dev/vars/main\\.yml$/"],
      "matchStrings": ["(?:dev_)?tflint_version:\\s*\"(?<currentValue>v[^\"]+)\""],
      "depNameTemplate": "terraform-linters/tflint",
      "datasourceTemplate": "github-releases"
    },
    {
      "customType": "regex",
      "description": "OpenTofu for both workflows' setup-opentofu; the workstation takes the zypper package instead. One depName, so both bump together; terraform.yml's destructive-plan gate guards the apply a bump fires, so minor/patch automerge.",
      "managerFilePatterns": ["/^\\.github/workflows/(lint|terraform)\\.yml$/"],
      "matchStrings": ["tofu_version:\\s*\"(?<currentValue>[^\"]+)\""],
      "depNameTemplate": "opentofu/opentofu",
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v(?<version>.+)$"
    },
    {
      "customType": "regex",
      "description": "Hugo for peaceiris/actions-hugo in the Firebase and go.sum autofix workflows; the workstation takes the zypper package instead.",
      "managerFilePatterns": ["/^\\.github/workflows/(firebase-hosting-(merge|pull-request)|hugo-gosum-autofix)\\.yml$/"],
      "matchStrings": ["hugo-version:\\s*\"(?<currentValue>[^\"]+)\""],
      "depNameTemplate": "gohugoio/hugo",
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v(?<version>.+)$"
    },
    {
      "customType": "regex",
      "description": "firebase-tools for the Firebase deploy workflows and the workstation; no zypper package. One depName, so the workflows and dev_firebase_tools_version bump together.",
      "managerFilePatterns": ["/^\\.github/workflows/firebase-hosting-(merge|pull-request)\\.yml$/", "/^roles/dev/vars/main\\.yml$/"],
      "matchStrings": ["firebase-tools@(?<currentValue>\\S+)", "dev_firebase_tools_version:\\s*\"(?<currentValue>[^\"]+)\""],
      "depNameTemplate": "firebase-tools",
      "datasourceTemplate": "npm"
    },
    {
      "customType": "regex",
      "description": "The ansible MCP server pinned in .mcp.json; no pip/zypper package.",
      "managerFilePatterns": ["/^\\.mcp\\.json$/"],
      "matchStrings": ["ansible-mcp-server@(?<currentValue>[^\"]+)"],
      "depNameTemplate": "@ansible/ansible-mcp-server",
      "datasourceTemplate": "npm"
    },
    {
      "customType": "regex",
      "description": "required_version constrains the OpenTofu CLI, but the terraform manager mis-tracks it as hashicorp/terraform (disabled below). hashicorp versioning parses the ~> range.",
      "managerFilePatterns": ["/^terraform/main\\.tf$/"],
      "matchStrings": ["required_version\\s*=\\s*\"(?<currentValue>[^\"]+)\""],
      "depNameTemplate": "opentofu/opentofu",
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v(?<version>.+)$",
      "versioningTemplate": "hashicorp"
    }
  ],
  "customDatasources": {
    "grafana-dashboards": {
      "defaultRegistryUrlTemplate": "https://grafana.com/api/dashboards/{{packageName}}",
      "format": "json",
      "transformTemplates": ["{ \"releases\": [ { \"version\": $string(revision) } ] }"]
    }
  },
  "packageRules": [
    {
      "description": "Grafana dashboard revisions are bare incrementing integers.",
      "matchDatasources": ["custom.grafana-dashboards"],
      "versioning": "regex:^(?<major>\\d+)$"
    },
    {
      "description": "pinDigest carries no version change, so the lint check is enough to merge it unattended.",
      "matchUpdateTypes": ["pinDigest"],
      "automerge": true
    },
    {
      "description": "Renovate reads no releaseTimestamp for a digest (renovate#38656), and minimumReleaseAgeBehaviour defaults to timestamp-required, so these park forever on a stability-days check no elapsed time clears. Sets no automerge, so the cadvisor/caddy rule below still holds.",
      "matchUpdateTypes": ["digest", "pinDigest"],
      "minimumReleaseAge": null
    },
    {
      "description": "No registry read here returns a releaseTimestamp, so a container version bump parks the same way. The rule above covers digests on the other datasources, where the github-actions pins live.",
      "matchDatasources": ["docker"],
      "minimumReleaseAge": null
    },
    {
      "description": "No unattended bump where a compromised image reads the host or fronts the internet. cadvisor runs as root with a read-only bind of the whole host filesystem; caddy terminates TLS for the public sites, holds the Cloudflare API token, and alone publishes 80/443. node_exporter is deliberately absent: it mounts the host D-Bus system bus, but runs as an unprivileged uid systemd refuses StartTransientUnit, and its molecule verify asserts that user. Must stay after the pinDigest rule — later rules win, and flatten reapplies packageRules after the update-type presets, so this has the last word.",
      "matchPackageNames": [
        "ghcr.io/google/cadvisor",
        "ghcr.io/caddybuilds/caddy-cloudflare"
      ],
      "automerge": false
    },
    {
      "description": "gitops_reconcile installs this ansible and applies the fleet as root, so an unattended bump is unread root code on every host. Hashing the pin would not help: pip's hash mode is per-invocation, so it would force a hashed closure onto requirements-dev.txt too.",
      "matchPackageNames": ["ansible"],
      "automerge": false
    },
    {
      "description": "linuxserver images carry legacy build-number, two-part and four-part tags that docker versioning reads as upgrades; their releases are strict semver.",
      "matchPackageNames": ["lscr.io/linuxserver/**"],
      "versioning": "semver"
    },
    {
      "description": "lidarr has a legacy 8.1.2135 tag that even strict semver accepts; mainline is 3.x.",
      "matchPackageNames": ["lscr.io/linuxserver/lidarr"],
      "allowedVersions": "<8"
    },
    {
      "description": "Every container here restarts on merge with something in flight to lose — plex mid-stream, transmission mid-torrent, sonarr/radarr mid-import. The plex-only window this replaces was the same rule scoped to one of the ten. Grouped, they restart in one window instead of scattered. Matched by file: the role's vars hold nothing else renovate manages, so a new container joins without a rule change.",
      "matchFileNames": ["roles/arr/vars/main.yml"],
      "groupName": "arr containers"
    },
    {
      "description": "google and google-beta release in lockstep, so two PRs are the same bump twice. matchDatasources keeps this off google-github-actions/auth.",
      "matchDatasources": ["terraform-provider"],
      "matchPackageNames": ["hashicorp/google", "hashicorp/google-beta"],
      "groupName": "google terraform providers"
    },
    {
      "description": "Follows renovate's own release cadence, several a day — a fifth of all renovate traffic here for a lint-gate pin with no reach onto the fleet. schedule constrains creation, where automergeSchedule constrains landing.",
      "matchPackageNames": ["renovatebot/pre-commit-hooks"],
      "schedule": ["before 5am on monday"]
    },
    {
      "description": "Hard dep of hetzner.hcloud's vendored SDK, which imports dateutil.parser.isoparse. Stable, not abandoned — just release-inactive.",
      "matchPackageNames": ["python-dateutil"],
      "abandonmentThreshold": null
    },
    {
      "description": "config:best-practices skips indirect gomod deps, so enable them for this go.mod or the Blowfish theme never updates. site-gate catches a break, and hugo-gosum-autofix completes go.sum and re-triggers it.",
      "matchManagers": ["gomod"],
      "matchFileNames": ["jonnyoc-site/go.mod"],
      "matchDepTypes": ["indirect"],
      "enabled": true
    },
    {
      "description": "required_version is the OpenTofu CLI, not HashiCorp Terraform; the custom manager above tracks it.",
      "matchManagers": ["terraform"],
      "matchPackageNames": ["hashicorp/terraform"],
      "enabled": false
    }
  ]
}
