{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":automergeMinor",
    ":enablePreCommit",
    ":semanticCommitScopeDisabled",
    ":semanticCommitTypeAll(build)"
  ],
  "semanticCommits": "enabled",
  "packageRules": [
    {
      "description": "requirements.txt deps reach the live fleet but CI runs lint only, not molecule, so they are merged by hand after a local molecule check.",
      "matchFileNames": ["requirements.txt"],
      "automerge": false
    }
  ]
}
