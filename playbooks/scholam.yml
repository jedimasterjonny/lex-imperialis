---
- name: Configure this host
  hosts: this_host
  roles:
    - common
    - sshd
    - firewalld
    - podman
    - autoupdate
    - nfs
    - incus
    - libvirt
    - dev
    - home_backup
    - node_exporter
    - gitops_reconcile
