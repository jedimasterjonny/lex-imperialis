[defaults]
inventory = inventory
roles_path = roles
interpreter_python = auto_silent
# Enable once .vault_pass exists; a missing file here fails every command.
# vault_password_file = .vault_pass

[ssh_connection]
pipelining = True
