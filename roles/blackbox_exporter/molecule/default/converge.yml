---
- name: Converge
  hosts: all
  tasks:
    - name: Apply the podman role
      ansible.builtin.include_role:
        name: podman

    - name: Apply the blackbox_exporter role
      ansible.builtin.include_role:
        name: blackbox_exporter
