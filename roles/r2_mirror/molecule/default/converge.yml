---
- name: Converge
  hosts: all
  tasks:
    # No vars: the container declaration comes from the scenario's group_vars, so
    # this and verify read the same set and cannot drift apart.
    - name: Apply the r2_mirror role
      ansible.builtin.include_role:
        name: r2_mirror
