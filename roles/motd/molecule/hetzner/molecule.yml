---
platforms:
  - name: motd-hetzner
    server_type: cx23
    image: opensuse-16
    # Pinned region: cx23 is in stock in nbg1 (also hel1, fsn1). If a run
    # fails with resource_unavailable, switch location to another in-stock one.
    location: nbg1
provisioner:
  name: ansible
  config_options:
    defaults:
      # Scenario dir is roles/motd/molecule/hetzner; ../../.. is roles/.
      roles_path: "${MOLECULE_SCENARIO_DIRECTORY}/../../.."
      # ../../../.. is the repo root, where the gitignored vault password lives.
      vault_password_file: "${MOLECULE_SCENARIO_DIRECTORY}/../../../../.vault_pass"
    ssh_connection:
      # opensuse cloud images run sshd without the sftp subsystem; piped skips
      # the failed sftp/scp attempts Ansible would otherwise warn about.
      transfer_method: piped
