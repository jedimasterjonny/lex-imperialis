{#- The line exists only for a key that is set: a blank <APP>__AUTH__APIKEY= is
    an empty key the app would honour, where no line at all leaves it its own. -#}
{% if item.value %}
{{ item.key | upper }}__AUTH__APIKEY={{ item.value }}
{% endif %}
