---
# renovate's image manager bumps the tag; the compose template renders it. The
# tag stays bare 'cli' (not semver) so renovate digest-pins it to the current
# docker CLI, as the wordpress role does for wp-cli.
docker_prune_image: docker.io/library/docker:cli@sha256:000bb62ff495f986c9f5578eb67cc2cb98b91138eda81d7762d5371eb8a497fe
