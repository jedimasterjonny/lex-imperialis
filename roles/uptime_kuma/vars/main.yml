---
# renovate's image manager bumps the tag. slim: no Chromium, so no real-browser
# monitors — a headless Chrome on a Raspberry Pi to assert what an HTTP monitor
# already can — and no apprise, whose notification bridge Discord does not need.
# rootless: the process runs as node, not root.
uptime_kuma_image: docker.io/louislam/uptime-kuma:2.5.4-slim-rootless@sha256:aad7432d2b64767fd9b0cada47831898330ab931bbdebf68b73cc919df3da1ff
