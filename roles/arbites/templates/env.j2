# {{ ansible_managed }}
# The GitHub token the deployment record is posted with. Read by both the reconcile
# script and its ExecStopPost hook, which inherits the service's environment.
ARBITES_GITHUB_TOKEN="{{ arbites_github_token }}"
