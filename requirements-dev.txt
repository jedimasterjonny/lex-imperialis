-r requirements.txt
ansible-lint==26.6.0
molecule==26.6.0
pre-commit==4.6.1
# pytest: drives the roles' molecule verify (the shim runs it on the controller).
pytest==9.1.1
# requests + python-dateutil: hetzner.hcloud's runtime deps, for the hetzner
# molecule scenario (the collection ships in ansible but not these).
python-dateutil==2.9.0.post0
requests==2.34.2
# ruff: lints and formats the pytest verify suites, run from the venv via pre-commit.
ruff==0.16.0
shellcheck-py==0.11.0.1
yamllint==1.38.0
